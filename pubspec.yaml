name: remote_sensors_app
description: Aplicativo para consulta da temperatura de um ambiente através de uma API na nuvem e um microcontrolador esp32

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  get: ^3.24.0
  dio: ^3.0.10
  charts_flutter: ^0.9.0
  fl_chart: 0.12.2
  cron: ^0.2.4
  meta: ^1.2.4
  intl: ^0.16.1
flutter:
  uses-material-design: true
